@inherits TwilightImperiumBaseComponenet

<img src="@(ImagePath)"
     alt="icon for @(UnitName)"
     title="@(UnitName)"
     class="unit-image"
     style="width: @(Width)%; max-width: @(MaxWidth)%;"/>
